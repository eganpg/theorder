<%= form_for @customer do |l| %>
	Name: <%= l.text_field :name %>
	Address: <%= l.text_field :address %>
	Email: <%= l.text_field :email %>
	Phone: <%= l.text_field :phone %>
	Add: <%= l.submit "Add" %>
	<% end %>

	<%= link_to "Back to the List", customers_path %>